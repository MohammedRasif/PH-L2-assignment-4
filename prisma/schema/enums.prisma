enum ActiveStatus {
  ACTIVE
  BLOCKED
}

enum Role {
  TENANT
  LANDLORD
  ADMIN
}

enum RequestStatus {
  PENDING
  APPROVED
  REJECTED
  COMPLETED
}

enum PaymentStatus {
  PENDING
  COMPLETED
  FAILED
}

enum PaymentProvider {
  STRIPE
  SSLCOMMERZ
}
