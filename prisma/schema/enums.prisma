enum ActiveStatus {
  ACTIVE
  BLOCKED
}

enum Role {
  TENANT
  LANDLORD
  ADMIN
}

enum RequestStatus {
  PENDING
  APPROVED
  REJECTED
}

enum PaymentStatus {
  PENDING
  COMPLETED
  FAILED
}

enum PaymentProvider {
  STRIPE
  SSLCOMMERZ
}
